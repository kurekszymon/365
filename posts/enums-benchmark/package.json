{
  "name": "enums-benchmark",
  "version": "1.0.0",
  "scripts": {
    "sizes": "bun scripts/bundle-sizes.ts",
    "treeshake": "bun scripts/tree-shaking.ts",
    "typecheck": "tsc --noEmit",
    "typecheck:tsc": "tsc --noEmit --project tsconfig.tsc.json"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "esbuild": "^0.25.0",
    "rolldown": "^1.0.2",
    "typescript": "^5.8.0"
  }
}
